{
  "name": "digital-card",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @digitalcard/web dev",
    "build": "pnpm --filter @digitalcard/web build",
    "db:push": "pnpm --filter @digitalcard/web db:push",
    "db:studio": "pnpm --filter @digitalcard/web db:studio",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  }
}
